export function generateMetadata() {
    return {
        title: "FitFocus - Sign Up",
        description: "Sign up for a FitFocus account to access exclusive content and features.",
    }
  }

  export default async function BMI({ children }) {
    return (
        <main>{children}</main>
    );
}