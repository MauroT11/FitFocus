export function generateMetadata() {
    return {
        title: "FitFocus - Sign In",
        description: "Sign in to your FitFocus account to access exclusive content and features.",
    }
  }

  export default async function BMI({ children }) {
    return (
        <main>{children}</main>
    );
}